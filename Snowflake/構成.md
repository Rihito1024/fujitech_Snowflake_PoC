# Snowflake PoC 構成メモ

## 全体アーキテクチャ

```mermaid
flowchart LR
    SF["Salesforce"]
    S3[("S3 バケット<br/>先方 AWS 環境")]

    SF -->|"エクスポート"| S3
    SF -.->|"Openflow 直接連携"| OF

    subgraph SNOW["Snowflake（単一環境 / dev・stg・prod 分離なし）"]
        direction TB
        subgraph DSDB["DATASOURCE DB"]
            OF["Openflow"]
            EXT["外部ステージ<br/>DATASOURCE.SALESFORCE"]
            RAW["Raw テーブル<br/>DATASOURCE.SALESFORCE"]
            OF --> RAW
            EXT --> RAW
        end
        subgraph SALESDB["SALES DB"]
            INT["ANALYTICS_INTERMEDIATE<br/>中間モデル"]
            MARTS["ANALYTICS_MARTS<br/>dim / fact / bridge / gold<br/>+ Semantic View"]
            ANA["ANALYTICS<br/>dbt プロジェクト / Task / Agent"]
            INT --> MARTS
        end
        RAW --> INT
        ANA -. "実行" .-> INT
        ANA -. "実行" .-> MARTS
    end

    S3 --> EXT
    MARTS --> TAB["Tableau"]
    MARTS --> CW["Snowflake Intelligence<br/>（CoWork / Cortex Analyst エージェント）"]
```

主なワークストリーム:

```mermaid
flowchart TB
    A["Snowflake 環境構築<br/>sql/00〜06"] --> B["営業パイプライン構築<br/>dbt 作成 → デプロイ → Task → 数値検証"]
    A --> C["Tableau 接続作成"]
    A --> D["CoWork 作成<br/>Semantic View + エージェント"]
    A --> E["Openflow 作成<br/>ランタイム / 連携先"]
    F["先方依頼<br/>S3 作成 / Salesforce サービスユーザー"] --> A
    E --> B
```

## やること
- Snowflakeの環境構築
- 営業パイプライン構築
    - dbt作成
    - デプロイ
    - タスク作成
    - 数値検証
- Tableau接続作成
- CoWork作成
- Openflow作成
    - ランタイム作成
    - 連携先作成


## 先方への依頼
- S3作成（先方AWS環境）
- Salesforceサービスユーザー設定


### Snowflake環境構築
#### DB配下

```mermaid
flowchart TB
    subgraph DATASOURCE
        C1["common<br/>Storage Integration 等の共通オブジェクト"]
        C2["Salesforce<br/>table / Openflow / 外部ステージ"]
    end
    subgraph SALES
        M1["Analytics_marts<br/>table / SemanticView"]
        M2["Analytics_intermediate<br/>table"]
        M3["Analytics<br/>dbt / Task / Agent"]
    end
    C2 --> M2 --> M1
    M3 -. "build" .-> M2
    M3 -. "build" .-> M1
```

- Datasource
    - common
    - Salesforce
        - table
        - Openflow
        - 外部ステージ
- Sales
    - Analytics_marts
        - table
        - SemanticView
    - Analytics_intermediate
        - table
    - Analytics 
        - dbt
        - Task
        - Agent

#### アカウントレベルオブジェクト
- WH
    - Sales_WH（dbt実行、営業ユーザー、Tableau共通）
    - Datasource_WH（S3 COPY等のアドホック取り込み）
    - Openflow_WH（Openflow の Salesforce CDC 定常負荷専用。アドホックと分離）
- RM
    - Sales_RM / Datasource_RM / Openflow_RM
        - クレジット上限・アラート通知先（Notification Integration）は要設定
- Integration
    - Storage Integration（S3用、IAMロール・外部ID）※Datasource/common用
        - 未実装・要検討
    - External Access Integration / Network Rule（Salesforce API疎通用）※Datasource/Salesforce用
        - 未実装・要検討
    - Notification Integration（WHアラート通知、タスク失敗時の通知先（メール/Slack等）に使用）→ 要設定
- Role

```mermaid
flowchart BT
    R["&lt;DB&gt;__r（参照）"] --> RW["&lt;DB&gt;__rw（参照+更新）"] --> RWM["&lt;DB&gt;__rwm（+作成/管理）"]

    R --> USER["&lt;DB&gt;_user"]
    RW --> DEV["&lt;DB&gt;_developer"]
    RWM --> MGR["&lt;DB&gt;_manager"]

    USER --> DEV --> MGR --> ADMIN["&lt;DB&gt;_Admin"]
    ADMIN --> SYSADMIN[("SYSADMIN")]

    WHU["Sales_WH_USAGE<br/>USAGE"] --> USER
    WHU --> DEV
    WHU --> MGR
    WHU --> ADMIN
    WHA["Sales_WH_ADMIN<br/>OPERATE / MODIFY"] --> ADMIN

    classDef access fill:#e8f0fe,stroke:#4285f4;
    classDef func fill:#e6f4ea,stroke:#34a853;
    classDef wh fill:#fef7e0,stroke:#fbbc04;
    class R,RW,RWM access;
    class USER,DEV,MGR,ADMIN func;
    class WHU,WHA wh;
```

アクセスロール（`__r ⊂ __rw ⊂ __rwm`）をファンクショナルロールにぶら下げ、ファンクショナルロールは
`user → developer → manager → Admin` の順に内包する。DATASOURCE / SALES の2系統を同じ形で作成。

    - Adminロールに他のファンクショナルロールはぶら下げる
    - WHのUSE権限は全ファンクショナルロールにつけるが、Manage権限はAdminだけ
    - アクセスロール
        - <DB>__r
        - <DB>__rw
        - <DB>__rwm
    - ファンクショナルロール
        - <DB>_Admin
        - <DB>_manager
        - <DB>_developer
        - <DB>_user
    - WHのロール
    - Future Grants：GRANT ... ON FUTUREでスキーマ単位に自動付与
    - アクセス範囲：スキーマ単位では分けず、DB単位で一律付与
    - Openflow（SPCSコンピュートプール、Datasource/Salesforce配下オブジェクト）のオーナーシップは既存の<DB>_Adminロールに持たせる
    - Openflow の 管理/運用（人間）は既存の DATASOURCE ロール階層に寄せ、ランタイム実行だけ専用ロール（05_salesforce_external_access.sql）:
        - `DATASOURCE_ADMIN` … 構築系。デプロイメント作成/アップグレードに必要なアカウントレベル権限（`CREATE COMPUTE POOL` / `CREATE INTEGRATION` / `BIND SERVICE ENDPOINT` / `CREATE APPLICATION` / `MANAGE GRANTS` 等）。実質アカウント管理者級に格上げされる点に注意（SYSADMIN にも継承）。許容できなければ初回のみ付与→完了後 REVOKE
        - `DATASOURCE_MANAGER` … 運用系（コネクタ操作・キャンバス編集・ログ確認）＋ UI ログイン。`SALESFORCE_ACCESS_INT` USAGE（UI でランタイムに割当）/ `OPENFLOW_WH` USAGE（手動確認）/ アプリロール `<app>.OPENFLOW_ADMIN`
        - `DATASOURCE_OPENFLOW` … ランタイム実行ロール（作成時に「実行ロール」として指定）。ランタイムのプロセスがこのロールでデータ操作。人間はログインに使わない。`DATASOURCE__RWM` 継承（DATASOURCE.SALESFORCE 書き込み・宛先テーブル自動作成）＋ `SALESFORCE_ACCESS_INT` USAGE ＋ `SALESFORCE_CREDENTIALS` READ ＋ `OPENFLOW_WH` USAGE ＋ コンピュートプール USAGE。dbt 側の `SALES_TRANSFORMER` と対
        - UI ログインは `DATASOURCE_MANAGER`（構築時のみ `DATASOURCE_ADMIN`）。ACCOUNTADMIN/SECURITYADMIN/ORGADMIN は OAuth で既定ブロックされ UI ログイン不可。ログインに使うのは各ユーザーの `DEFAULT_ROLE`
        - ランタイムオブジェクト `Openflow_Runtime`（対象スキーマ DATASOURCE.SALESFORCE）の Manage access は UI で設定済み。USAGE / OPERATE / MONITOR / USAGE ON SCHEMA / CREATE OPENFLOW CONNECTOR ON SCHEMA / USAGE ON DATABASE をすべて `DATASOURCE_MANAGER` のみに付与
- User
    - Tableauサービスユーザー
        - 認証方式はPAT
    - 営業ユーザー（人を想定）
- ネットワークポリシー（IP制限）、SSO/SAML連携 → 一旦パス（PoCのため）

#### ロールとオブジェクトのマッピング（現状の sql/01〜08）

ロール階層（矢印 = grant されて上位が内包）:

```mermaid
flowchart BT
    D__R["DATASOURCE__R"] --> D__RW["DATASOURCE__RW"] --> D__RWM["DATASOURCE__RWM"]
    S__R["SALES__R"] --> S__RW["SALES__RW"] --> S__RWM["SALES__RWM"]

    D__R --> DU["DATASOURCE_USER"]
    D__RW --> DD["DATASOURCE_DEVELOPER"]
    D__RWM --> DM["DATASOURCE_MANAGER<br/>+ Openflow 運用 / UIログイン"]
    DU --> DD --> DM --> DA["DATASOURCE_ADMIN<br/>+ Openflow 構築（アカウントレベル権限）"]

    S__R --> SU["SALES_USER"]
    S__RW --> SD["SALES_DEVELOPER"]
    S__RWM --> SM["SALES_MANAGER"]
    SU --> SD --> SM --> SA["SALES_ADMIN"]

    S__RWM --> TF["SALES_TRANSFORMER<br/>dbt実行 / SVC_DBT"]
    D__R --> TF
    D__RWM --> OFR["DATASOURCE_OPENFLOW<br/>Openflowランタイム実行"]

    DA --> SYS[("SYSADMIN")]
    SA --> SYS
    TF --> SA
    TF --> SYS
    OFR --> DA
    OFR --> SYS

    classDef access fill:#e8f0fe,stroke:#4285f4
    classDef func fill:#e6f4ea,stroke:#34a853
    classDef x fill:#fff4e5,stroke:#fb8c00
    class D__R,D__RW,D__RWM,S__R,S__RW,S__RWM access
    class DU,DD,DM,DA,SU,SD,SM,SA func
    class TF,OFR x
```

ウェアハウス ↔ ロール:

```mermaid
flowchart LR
    SWH["SALES_WH"] -->|USAGE| SWU["SALES_WH_USAGE<br/>= 全ファンクショナルロール + SALES_TRANSFORMER"]
    SWH -->|OPERATE/MODIFY| SWA["SALES_WH_ADMIN<br/>= DATASOURCE_ADMIN / SALES_ADMIN"]
    DWH["DATASOURCE_WH"] -->|USAGE| DWU["DATASOURCE_WH_USAGE<br/>= DATASOURCE_USER〜ADMIN"]
    DWH -->|OPERATE/MODIFY| DWA["DATASOURCE_WH_ADMIN<br/>= DATASOURCE_ADMIN"]
    OWH["OPENFLOW_WH"] -->|USAGE| OWU["DATASOURCE_OPENFLOW（ランタイム実行）<br/>+ DATASOURCE_MANAGER（手動確認）"]
    OWH -->|OPERATE/MODIFY| DA["DATASOURCE_ADMIN"]

    SWH -.->|RESOURCE_MONITOR| SRM["SALES_RM"]
    DWH -.->|RESOURCE_MONITOR| DRM["DATASOURCE_RM"]
    OWH -.->|RESOURCE_MONITOR| ORM["OPENFLOW_RM"]
```

名前付きオブジェクト ↔ ロール（所有 / 権限）:

```mermaid
flowchart LR
    DSDB["DATASOURCE DB + COMMON / SALESFORCE スキーマ"] -->|OWNERSHIP| DA["DATASOURCE_ADMIN"]
    SLDB["SALES DB + ANALYTICS_INTERMEDIATE / _MARTS / ANALYTICS"] -->|OWNERSHIP| SA["SALES_ADMIN"]

    S3INT["SALESFORCE_S3_INT (Storage Integration)"] -->|USAGE| D__RWM["DATASOURCE__RWM"]
    STG["EXT_STAGE / CSV_DEFAULT (04)"] -->|OWNER| DM["DATASOURCE_MANAGER"]
    EAI["SALESFORCE_ACCESS_INT (EAI)"] -->|USAGE| DM
    EAI -->|USAGE| OFR["DATASOURCE_OPENFLOW（ランタイム実行）"]
    SEC["SALESFORCE_CREDENTIALS (Secret) ※未作成"] -->|"READ（予定）"| OFR
    RT["OPENFLOW RUNTIME Openflow_Runtime<br/>USAGE / OPERATE / MONITOR / CREATE OPENFLOW CONNECTOR"] -->|付与| DM

    SI["SNOWFLAKE_INTELLIGENCE_OBJECT_DEFAULT"] -->|MODIFY| SA
    SI -->|USAGE| S__R["SALES__R"]
    SV["ANALYTICS_MARTS.SV_OPPORTUNITY_LINE_ITEM"] -->|OWNER| SM["SALES_MANAGER"]
    SV -->|SELECT| S__R
    AG["ANALYTICS.SALES_PIPELINE_AGENT"] -->|OWNER| SM
    AG -->|USAGE| S__R
    GOLD["ANALYTICS_MARTS.GOLD_OPPORTUNITY_LINE_ITEM_WIDE<br/>+ LOAD_STAGE / CSV_JA_HEADER (07)"] -->|OWNER| SM
    CAU["SNOWFLAKE.CORTEX_AGENT_USER (DBロール)"] -->|GRANT| SUSER["SALES_USER"]
    DBTOBJ["dbt 生成オブジェクト（ANALYTICS_INTERMEDIATE / _MARTS）"] -->|OWNER| TF["SALES_TRANSFORMER"]
```

ユーザー:

| ユーザー | 種別 | DEFAULT_ROLE | DEFAULT_WH | 認証 |
| --- | --- | --- | --- | --- |
| `SVC_TABLEAU` | SERVICE | `SALES_USER` | `SALES_WH` | PAT（`NETWORK_POLICY = TABLEAU_PAT_NP`） |
| `SVC_DBT` | SERVICE | `SALES_TRANSFORMER` | `SALES_WH` | キーペア推奨 |
| `SVC_OPENFLOW` | SERVICE | `DATASOURCE_OPENFLOW` | `OPENFLOW_WH` | RSA キーペア（NP 不要） |
| 営業ユーザー（人・テンプレ） | PERSON | `SALES_USER` | `SALES_WH` | — |
| Openflow 担当（人） | PERSON | `DATASOURCE_MANAGER`（構築時 `DATASOURCE_ADMIN`） | — | Snowflake OAuth（UI ログイン） |

※ Openflow ランタイムの実行ロールは `DATASOURCE_OPENFLOW`。Salesforce コネクタが Snowflake へ書き込む接続ユーザー `SVC_OPENFLOW` も同じロールにして権限を1本化（06_users.sql）。

#### 環境
- dev/staging/prod分離はせず、単一環境で運用

#### データ取り込み

```mermaid
flowchart LR
    subgraph SEQ["先行: S3 経由"]
        SF1["Salesforce"] --> B1[("S3")] --> STG["外部ステージ<br/>STORAGE INTEGRATION"] -->|"COPY INTO"| RAW1["DATASOURCE.SALESFORCE 配下テーブル"]
        RAW1 --> DBT["dbt 開発"]
    end
    subgraph PAR["並行: Openflow 直接接続"]
        SF2["Salesforce API"] -->|"External Access Integration<br/>+ Network Rule"| OFR["Openflow ランタイム<br/>SPCS コンピュートプール"] --> RAW2["DATASOURCE.SALESFORCE 配下テーブル"]
    end
```

- S3経由とOpenflow直接接続の両方を使う
    - まずS3から外部ステージ経由でデータを取り込み、dbt開発を先行して進める
    - Openflowの構築（ランタイム・連携先）は並行して進める
- 未実装・要検討
    - 外部ステージのファイルフォーマット定義 → 実ファイルを見て作成
    - 自動取り込み方式 → 一旦COPY INTO（Snowpipe等は後で検討）
    - Openflowランタイム用SPCSコンピュートプール → GUIで作成

#### 個人情報保護
- マスキングポリシー等の対応は不要（対象データに個人情報を含まない前提）

#### タスク/監視

```mermaid
flowchart LR
    ROOT["取り込み完了<br/>（COPY INTO / Openflow）"] --> STG["dbt run（staging）<br/>stg_*"]
    STG --> MART["dbt run（mart）<br/>dim_* / fact_* / bridge_*"]
    MART --> GOLD["gold_opportunity_wide<br/>gold_opportunity_line_item_wide"]
    GOLD --> TEST["dbt test<br/>数値検証は人力"]
    STG -. "失敗" .-> NOTIFY["Notification Integration<br/>メール / Slack"]
    MART -. "失敗" .-> NOTIFY
    GOLD -. "失敗" .-> NOTIFY
```

- タスクの依存関係（DAG）設計
- タスク失敗時の通知先（メール/Slack等のNotification Integration）
- 数値検証は人力（自動化しない）

#### CoWork

```mermaid
flowchart LR
    U["営業ユーザー"] -->|"自然言語で質問"| AG["Cortex Agent<br/>SALES.ANALYTICS"]
    AG -->|"Tool 呼び出し"| CA["Cortex Analyst"]
    CA -->|"参照"| SV["Semantic View<br/>SALES.ANALYTICS_MARTS"]
    SV -->|"クエリ"| MARTS[("gold_* / dim_* / fact_*")]
```

- Snowflake Intelligenceを使用
- SalesforceデータにSemantic View（SV）を作成し、そのCortex AnalystをToolとして呼び出すエージェント構成

#### その他 要検討
- Time Travel保持日数 → 一旦パス（PoCのため）
